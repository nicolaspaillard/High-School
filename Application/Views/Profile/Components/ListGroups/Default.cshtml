@using Models
@model List<Group>
@using Application.Controllers.ViewComponents
@using Application.Controllers.Services
@inject RepositoryService<Group> GroupServices


<button type="button" class="btn btn-success" data-toggle="ajax-modal" data-target="#create-group" data-url="@Url.Action("CreateGroupModal")">
    Créer une classe
</button>
<div id="modal-placeholder"></div>
<table class="table">
    <thead>
        <tr>
            <th>
                Groupe
            </th>
            <th>
                Etudiants
            </th>
            <th>
                Cours
            </th>
            <th>
                Professeur principal
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => @item.GroupID)
                </td>
                <td>
                    @item.Students.Count()
                </td>
                <td>
                    @item.Courses.Count()
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HomeRoomTeacher.LastName)
                </td>
                <td>
                    <button type="button" class="btn btn-secondary" data-toggle="ajax-modal" data-target="#edit-group" data-url="@Url.Action("EditGroupModal","Profile",new { GroupID = item.GroupID.ToString() })">
                        Modifier
                    </button>
                    <button type="button" class="btn btn-danger" href="#modal-placeholder">
                        Supprimer
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>
